# Rules and statistical analysis

The framework combines deterministic prioritization rules with a
`DensityFunction` model. Rules expose their inputs and thresholds for analyst
review; the statistical model highlights observations that differ from its
learned score distribution. Neither output is a verdict, and both require
contextual investigation.
