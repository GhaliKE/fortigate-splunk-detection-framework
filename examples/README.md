# Synthetic examples

The CSV in this directory is fully synthetic and uses only RFC 5737
documentation addresses. It checks normalization and prioritization logic
with four aggregate scenarios. It does not contain organizational logs and
cannot measure real-world detection performance.
