# Dashboards

- `fortigate_global.xml`: global FortiGate traffic monitoring.
- `sensitive_access.xml`: SSH, RDP, SMB, and SSL/VPN access monitoring.
- `ssh_threat_hunting.xml`: focused SSH investigation.

Import each XML as a private Classic XML dashboard, then review time ranges,
permissions, field names, and the target index before sharing it.
